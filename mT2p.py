# import the wxPython GUI package
import wx
import mT2pClasses

app = mT2pClasses.mT2pApp(0)     # Create an instance of the application class
app.MainLoop()     # Tell it to start processing events